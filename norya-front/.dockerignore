# Nunca levar para o build context — o Dockerfile faz `COPY . .` e um
# .env.local presente seria embutido no bundle do client (NEXT_PUBLIC_*).
.env.local
.env*.local
node_modules
.next
.git
.stale_nm_root_owned
.stale_next_root_owned
tsconfig.tsbuildinfo
dump.rdb
*.pdf
